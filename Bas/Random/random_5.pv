size(200, 200)
background(0)

for i in range(100):
    x = random(40)
    y = random(1)
    nofill()
    stroke(10)  
    oval(10,random(i+10), random(i+10), 40)
 
    stroke(0) 
    oval(random(i*50), random(y+i), random(i), 300)