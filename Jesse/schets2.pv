size(200, 200)
background("white")

for i in range(60):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill('black')
    rotate(40)
    (x, y, 50, 50)
    
    fill('red', 'black')
    stroke(10)
    rect(x, y, 50, 50)