size(200, 200)
background(1)
stroke (0)
font ("futura", 10)

# start position
y = 5
x = 5

for j in range(20):
    y = 10
    x = 10 + (j*25)
    
    for i in range(20):
        f = 5 + (i*random(5))
        y2 = y + (WIDTH - 10) /10
        x2 = x + random(20) - 10
        font ("futura", f)
        text ("z", x, y, x2, y2)
        y = y2
        x = x2