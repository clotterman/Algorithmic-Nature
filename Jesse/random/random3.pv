size(200, 200)
background("black")

for i in range(30):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill('black')
    rotate(80)
    (x, y, 60, 60)
    
    fill('black')
    stroke(10)
    oval(x, y, 50, 50)