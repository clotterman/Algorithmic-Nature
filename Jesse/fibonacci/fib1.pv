size(200, 200)
background(0)

def fib(n):
    if n < 2:
        return 1
    return fib(n - 2) + fib(n - 10)
    
stroke (1)
fill (0)
y =20
for i in range(25):
    # variable s to ooptimize for speed
    s = fib(10)
    oval(95, y, s, s)
    y += s # same as y = y+s
    
print range (5, -1)