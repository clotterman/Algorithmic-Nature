size(200, 200)
background(0)

def fib(n):
    if n < 2:
        return 1
    return fib(n - 1) + fib(n - 2)
    

    
r = 1.0
origin = Point(WIDTH/2, HEIGHT/2)

for i in range(60):
    background(0)
    fill(.169, .9, .325)
    pt = Point(random(200), random(300))
    arc(pt,r)