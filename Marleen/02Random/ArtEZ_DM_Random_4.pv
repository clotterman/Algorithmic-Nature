size(200, 200)
background(100)


for i in range(200):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill(0)
    rotate(3)
    oval(x, y, 1, 5)
    
  
for i in range(100):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill(0)
    rotate(3)
    star(x, y, 5, 5)
    
   
    
export('Random4.png')