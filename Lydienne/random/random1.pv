size(512, 512)
background(1)


for j in range(10):
    
    for i in range(200):
        rotate((random(WIDTH) * i) + (2 * j))
        x = random(WIDTH)
        y = random(HEIGHT)
        oval(x, y, random(), 13)
        reset()
