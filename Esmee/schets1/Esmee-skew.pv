size(200, 200)
background(1)

def fib(n):
    if n < 2:
        return 1
    return fib (n - 1) + fib(n - 2)
    
fill(0, 50, 0)
y = 20
for i in range(15):
    s = fib(i)
    print "%d: %d" % (i + 1, s)
    skew(2)
    rect(50, y, s, s)
    y += s