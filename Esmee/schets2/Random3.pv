size(200, 200)
background("black")

for i in range(40):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill('black')
    rotate(40)
    oval(x, y, 100, 100)
    
    fill('grey', 'black')
    stroke(10)
    oval(x, y, 50, 50)