size(200, 200)
background("pink")
words = ["Esmee", "en", "dit"]
colors = [color(1,0,0), color(0,1,1), color(0,0,1)]

for i in range(40):
    x = random(WIDTH)
    y = random(HEIGHT)
    rotate(random(360))
    fontsize(random(50,100))

    fill(choice(colors))
    text(choice(words), x, y)