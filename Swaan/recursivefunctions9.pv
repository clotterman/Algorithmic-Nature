size(600, 600)
background(1)
stroke (0.1)
shadow('#999', blur=2, offset=15)

x = 0
y = 400

for i in range(33):
    x2 = x + (WIDTH) / 30
    y2 = y + random(0.5) + 8
    line (random(), random(), x2, y2)

    x = x2
    y = y2
