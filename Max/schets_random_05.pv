size(200, 200)
background("black")


for i in range(200):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill('black')
    rotate(360)
    ellipse(x, y, 1, 1)
    
    fill('white', 'grey')
    rect(x, y, 1, 10)