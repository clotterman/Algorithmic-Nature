size(200, 200)
background("black")
words = ["C0SM0S", "C00S"]
colors = [color(1,1,1), color(1,1,1)]

for i in range(5):
    x = random(WIDTH)
    y = random(HEIGHT)
    rotate(random(360))
    fontsize(random(0,200))

    fill(choice(colors))
    text(choice(words), x, y)