size(500, 500)
background(random(0.9),random(0.9),random(0.8))

for i in range(200):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill(random(x), random(0.9), 0)
    oval(x, y, 10, 10)
    
    fill(random(0.7), random(0.9), random(0.9))
    rect(random(i), random(i+x+y*10), 10, 10)
    
export("1.png")