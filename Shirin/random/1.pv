size(500, 500)
background("black", "white")

for i in range(200):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill(1)
    oval(x, y, 10, 10)
    
    fill(0)
    rect(random(i), random(i+x+y*10), 10, 10)
    
export("1.2.png")
    
