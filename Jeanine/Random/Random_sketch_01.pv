size(200, 200)
background(0)

for i in range(200):
    x = random(WIDTH)
    y = random(HEIGHT)
    #rect(x, y, 20, 40)
    oval(x, y, 3, 3)
    #rect(x, y, -100, -1)
    rect(x, y, 100, 1)
    rect(x, y, 10, -1)
    #rect(x, y, -1, -30)
    fill(1)
