size(200, 200)
background(1)
stroke (0)

# start position
x = 10
y = 100

for i in range(20):
    x2 = x + (WIDTH - 20) / 20
    y2 = y + random(20) - 10
    line (x, y, x2, y2)
    line ((x + 500), (y + 5), x2, y2)
    
    x = x2
    y = y2
    