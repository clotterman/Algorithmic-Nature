
size(200, 200)
background(1)

for i in range(50):
    x = random(WIDTH)
    y = random(HEIGHT)
    oval(x, y, y, y)
    stroke(0)
    nofill()
    
for i in range(2):
    z = random(WIDTH)
    v = random(HEIGHT)
    oval(z, v, z, z)
    fill('red')
    nostroke()
    