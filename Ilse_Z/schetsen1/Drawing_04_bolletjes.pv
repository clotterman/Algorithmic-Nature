size(512, 512)
background(1)

def fib(n):
    if n < 2:
        return 1
    return fib(n - 1) + fib(n - 2)
    

    
r = 2.0
origin = Point(WIDTH/2, HEIGHT/2)

for i in range(70):
    background(.2, .35, .6)
    fill(.169, 0.9, .426)
    pt = Point(random(300), random(500))
    arc(pt,r)

