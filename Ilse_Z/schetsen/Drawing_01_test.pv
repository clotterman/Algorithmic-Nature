size(512, 512)
background(1)

with bezier(10, 10, stroke=0.1) as path:
    lineto(40, 10)
    
with bezier(25, 10, stroke=0.1) as path:
    lineto(25, 80)
    
with bezier(90, 10, stroke=0.1) as path:
    lineto(90, 80)
    
with bezier(110, 10, stroke=0.1) as path:
    lineto(90, 10)
    
with bezier(110, 40, stroke=0.1) as path:
    lineto(90, 40)
    
with bezier(110, 80, stroke=0.1) as path:
    lineto(90, 80)
    
with bezier(160, 10, stroke=0.1) as path:
    lineto(160, 40)
    
with bezier(160, 10, stroke=0.1) as path:
    lineto(190, 10)
    
with bezier(160, 40, stroke=0.1) as path:
    lineto(190, 40)
    
with bezier(190, 80, stroke=0.1) as path:
    lineto(190, 40)
    
with bezier(160, 80, stroke=0.1) as path:
    lineto(190, 80)
    
    
with bezier(220, 10, stroke=0.1) as path:
    lineto(250, 10)
    
with bezier(235, 10, stroke=0.1) as path:
    lineto(235, 80)
    
    
    
