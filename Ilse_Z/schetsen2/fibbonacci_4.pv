size(512, 512)
background(1)

def fib(n):
    if n < 2:
        return 1
    return fib(n - 1) + fib(n - 2)
    

    
r = 10
origin = Point(WIDTH/2, HEIGHT/2)

for i in range(200):
    background(.1, .10, .6)
    fill(.99, .2, .9)
    stroke(1)
    pt = Point(random(512), random(512))
    arc(pt,r)

