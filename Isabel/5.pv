import noise

size(200, 200)
background(1)
fill(0)

for y in range(0, WIDTH, 10):
    for x in range(0, WIDTH, 10):
        z = noise.snoise2((x * 0.01), (y * 0.01), octaves=4)
        # z = z * 20
        x_3d = x * 0.5 + y * 0.5
        y_3d = y * 0.8 - z * 0.5
        oval(x_3d, y_3d, 2, 2)