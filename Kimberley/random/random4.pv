size(200, 200)
background(1)
color(RGB)

kleur = color(RGB, random(), random(), 9)

for i in range(100):
    with bezier(430,10,fill=None):
        stroke(0.01)
        stroke(0)
        arcto(0, 200-i*10)
        
for i in range(200):
    with bezier(400,-1,fill=None):
        stroke(0.02)
        stroke(random())
        arcto(0, 1-i*9)


   
