size(200, 200)
background(1)

for i in range(100):     #(10) = aantal
    radius = random(1,14)
    origin = Point(WIDTH, HEIGHT)
    pt = Point(random(WIDTH), random(HEIGHT))
    fill()
    arc(pt,radius)
    line(origin, pt)
    stroke(0.1)

export('random5.png')