size(200, 200)
background("black")

for i in range(200):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill('black')
    rotate(360)
    rect(x, y, 100, 100)
    
    fill('white', 'white')
    oval(x, y, 10, 10)