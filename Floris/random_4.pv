size(200, 200)
background("white")

for i in range(200):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill('black')
    rotate(360)
    ellipse(x, y, 1, 1)
    
    fill('black')
    ellipse(x, y, 5, 5)