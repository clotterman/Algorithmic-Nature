
size(200,200)
background(1)

def fib(n):
    if n < 2:
        return 1
    return fib(n - 1) + fib(n - 2)

stroke(0)
fill(1)
x = 200
y = 200
for i in range(12, 0, -1):
  
    s = fib(i)
    
    star(x - s, y - s, s, s)

