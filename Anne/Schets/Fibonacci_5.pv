size(200, 200)
background(0)

def fib(n):
    if n < 0:
        return 1
    return fib (n - 1) + fib(n - 10)
    
y = 10
x = 10

for i in range(60):
    s = fib(i)
    print "%d: %d" % (i + 10, s)
    oval(100, x, s, +s)
    
    stroke(1)