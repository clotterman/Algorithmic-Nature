size(200, 200)
background(.7, .0, .10)

def fib (n):
    if n < 150:
        return 100
    return fib(n - 1) + fib(n - 2)
    
stroke(10)
fill(10, 40, 0)
background(.300, .60, .3)
x = 20
y = 30
for i in range(15):
    s = fib(i)
    print"%d: %d" % (i + 1, s)
    oval(x, y, s, s)
    y += s