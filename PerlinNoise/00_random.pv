# Print a random number between 0 and 5
print random(6)
print random(6)

# Print a random number between 1 and 6
print random(1,6)

# Print a random number 20 times
for i in range(20):
    print random(1,6)
    
# See documentation on random()
# .../plotdevice-docs/ref/Misc.html#random()

# Seed the random numbers from same exact number
from random import seed
seed(0)

for i in range(20):
    print random(1,6)