size(500, 500)
background("grey")

for i in range(200):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill(1)
    rect(x, y, 3, 10)
    
    fill(0)
    rect(random(i), random(i+x+y*10), 10, 10)
    

    
